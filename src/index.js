import React from "react";
import { createRoot } from "react-dom/client";
import SourdoughCalculator from "./SourdoughCalculator";

const root = createRoot(document.getElementById("root"));
root.render(<SourdoughCalculator />);
